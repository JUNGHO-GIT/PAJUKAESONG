// ImportUtils.tsx

import { dataArray } from "../assets/ts/dataArray.ts";
import {
  randomNumber, randomTime, calcDate, strToDecimal, decimalToStr, log
} from "../assets/ts/utils.ts";

// -------------------------------------------------------------------------------------------------
export {
  dataArray,
  randomNumber, randomTime, calcDate, strToDecimal, decimalToStr, log
};