// ImportUtils.tsx

import { dataArray } from "../assets/scripts/dataArray.ts";
import {
  randomNumber, randomTime, calcDate, strToDecimal, decimalToStr, log
} from "../assets/scripts/utils.ts";

// -------------------------------------------------------------------------------------------------
export {
  dataArray,
  randomNumber, randomTime, calcDate, strToDecimal, decimalToStr, log
};