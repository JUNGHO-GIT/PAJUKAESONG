// ImportUtils.tsx

import { dataArray } from "@scripts/dataArray";
import {
  randomNumber, randomTime, calcDate, strToDecimal, decimalToStr,
  log, makeFormData,
} from "@scripts/utils";

// -------------------------------------------------------------------------------------------------
export {
  dataArray,
  randomNumber, randomTime, calcDate, strToDecimal, decimalToStr,
  log, makeFormData,
};